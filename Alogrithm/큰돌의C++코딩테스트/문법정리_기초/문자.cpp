#include<bits/stdc++.h>

using namespace std;

int main(){
    ios::sync_with_stdio(0); cin.tie(0);

    char a = 0;
    char b = '\0';

    string str = string(1, b);

    if (a == b) cout << "a,b is the same" << "\n";
    if (str == "") cout << "str is empty\n";
    else "str is not empty\n";

    return 0;
}