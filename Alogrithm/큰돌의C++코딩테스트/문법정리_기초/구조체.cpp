#include<bits/stdc++.h>

using namespace std;

struct Apple {
    int y, x;
    Apple(int y, int x) : y(y), x(x) {

    }

    Apple() {
        y = -1;
        x = -1;
    }
};

int main(){
    ios::sync_with_stdio(0); cin.tie(0);

    return 0;
}