SELECT *
  FROM EMP
 WHERE NVL (COMM, 0) > 0;

SELECT *
  FROM EMP
 WHERE COMM > 0;

SELECT *
  FROM EMP
 WHERE COMM IS NULL;

SELECT *
  FROM EMP
 WHERE COMM IS NOT NULL;

SELECT *
  FROM EMP
 WHERE COMM > 0
    OR COMM IS NOT NULL;
